# Home Page Refinements — Three Changes

I need you to make exactly three changes to my Home page. Nothing else should be modified — the project cards, descriptions, CTA button, and footer all stay as they are.

---

## 1. Headline: Remove "· IE University"

**Current headline:**
```
Power Platform & Microsoft Copilot specialist · IE University
```

**New headline:**
```
Power Platform & Microsoft Copilot specialist
```

Just remove "· IE University". Keep the same font size, weight, and styling. IE University is already mentioned in the description paragraph below ("Currently leading Copilot adoption at IE University…"), so it still appears on the page — just not as part of my professional identity label.

---

## 2. Language toggle: Move from hero to navbar

There is currently a language toggle (EN | flag icon) sitting inside the hero section, above the headline. **Move it to the top navigation bar**, positioned next to the dark mode toggle (the moon icon).

Requirements:
- Remove the language toggle from its current position in the hero entirely.
- Place it in the navbar, immediately to the left or right of the dark mode moon icon.
- Format it as **plain text**: `EN | ES` — no flag icons. Flags are harder to read at small sizes and introduce ambiguity.
- The active language should be visually distinct (e.g., bolder, different color, or underlined) so the user knows which language is currently selected.
- It should remain visible at all times (same behavior as the navbar — sticky/fixed if the navbar is sticky/fixed).
- Functionality stays the same — clicking switches the site language.

---

## 3. Remove "Featured Work | Case Study" element from the hero

There is a segmented control / tab element in the hero section labeled "Featured Work" and "Case Study" that links to my first project. **Remove it entirely.**

Reason: it is redundant with the project cards immediately below the hero, and its function is ambiguous to first-time visitors. The project cards already serve as the entry point to each case study.

After removing it, the hero section should flow cleanly as:
1. Headline
2. Description paragraph
3. "Open to Microsoft Copilot / Power Platform consultant roles…" line
4. "Let's connect on LinkedIn →" button

No extra elements between the headline and the description.

---

## Summary of what stays unchanged

- The description paragraph ("I design and ship Copilot Studio agents…") → no changes.
- The "Open to…" availability line → no changes.
- The "Let's connect on LinkedIn →" CTA button → no changes.
- All four project cards (Immigration Agent, Visa Set Builder, Onboarding SPA, PaddockPlan) → no changes to order, content, or styling.
- Footer → no changes.
- All other pages (About, Projects, individual case studies) → no changes.
